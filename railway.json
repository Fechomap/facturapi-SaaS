{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo Building facturapi-SaaS V2 TypeScript && cd v2-typescript && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd v2-typescript && npx prisma migrate deploy && node dist/bot.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
